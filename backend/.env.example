PORT=4000
DATABASE_URL=postgres://postgres:your_password@localhost:5432/SIMLEARN
JWT_SECRET=replace-with-a-strong-secret
# Comma-separated list (or use CORS_ORIGINS). Include your LAN frontend origin when testing on another device/IP.
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000,http://192.168.1.12:3000
